#include <behaviors.dtsi>
#include <dt-bindings/zmk/bt.h>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/mouse.h>
#include <dt-bindings/zmk/pointing.h>
#include <behaviors/rgbled_widget.dtsi>

&trackball {
    automouse-layer = <2>;
    scroll-layers = <3>;
};


/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
                // pos0(リセット隣)  pos1=戻る  pos2=左クリック  pos3=スクロールトグル
                &bt BT_NXT  &mkp MB4  &mkp LCLK  &tog 3
            >;
	};

    mouse_key_layer {
            bindings = <
                &trans  &mkp MB4  &mkp LCLK  &tog 3
        >;
    };

    auto_mouse_layer {
            // トラックボール移動中。常に同じ割り当てにする
            bindings = <
                &trans  &mkp MB4  &mkp LCLK  &tog 3
        >;
    };

    scroll_layer {
            // スクロール中。pos3(key3)で解除(トグルOFF)、key2は進む
            bindings = <
                &trans  &mkp MB4  &mkp MB5  &tog 3
        >;
    };
    


    };

    combos {
        compatible = "zmk,combos";

        // pos1 + pos2 = (□ - ■ ■ □)
            // 進む

        combo_forward {
            bindings = <&mkp MB5>;
            key-positions = <1 2>;
            timeout-ms = <50>;
        };

        // pos2 + pos3 = (□ - □ ■ ■)
            // 右クリック

        combo_right_click {
            bindings = <&mkp RCLK>;
            key-positions = <2 3>;
            timeout-ms = <50>;
        };

        // pos0 + pos3 = (■ - □ □ ■)
            // 現在の接続先のBT接続情報を消去

        combo_bt_clr {
            bindings = <&bt BT_CLR>;
            key-positions = <0 3>;
            timeout-ms = <50>;
        };


    };

};
